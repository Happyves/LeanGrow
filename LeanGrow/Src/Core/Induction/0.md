

changes:
- `eq_def` theorems of WF-defs are among env constants now
- auxiliary proofs now have form `_proof_1` instead of `proof_1`, which makes them internal decalrations for the blacklisting based on names