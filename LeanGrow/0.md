

Reverse dags in embeddings so that sinks get explored first, so that the propagation phase is longer ?

Use Array.fold for efficiency

For names to label theorems and local contexts with, maybe restrain to const names of sinks of corresponding DAG, to prevent name lists from getting too big ?? Risk : trivial lemmas on sources of DAG get ignored ??

instead of constant names, use other data or encode it. If names get encoded, we should gain speedup in queires ?
