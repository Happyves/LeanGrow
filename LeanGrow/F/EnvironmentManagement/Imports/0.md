

The plan is to do the following

- make an initialization command that looks up the current imports, loads the relevant LeanGrow preprocess files into the environement, and add dummy declarations to environement that will be lists of the trie-like structures of the added cache imports

- grow should then start by quering the environement for these dummy declarations, by their dummy name, throw an error if they're not found, or if they're wierd, and otherwise proceed with them.

- By the looks of things now, the only effect of this modular approach is that we should always expect lists of trie-like structures when querying for thms or states.

- Maybe use better structure then lists, for coherence among indices of trie-structures ??

- We should also handle training : which trie-like structures of which files to modify and how