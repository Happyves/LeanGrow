
For samples, store thm implied goal, but also topmost goal ? This should be useful for generation of sensitive types ?



In sampling of sensitive type data : function application may be partial, as there are cases where function type is requires in proofs, so we must expect indices to be potentially out of bounds after use of Expr.getAppArgs and attempt to get them via Array.get!
+ Insight : its best to consider senstive types a sampling time : if in a recursor, the motive isn't Prop valued (example a recursive fucntion), then the cases which would correspond to base and induction cases are senstive types too, unlike if the motive is prop valued
