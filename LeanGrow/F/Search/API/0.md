

It would be good to cluster local contexts when they get big somehow ??
- co-clustering (CombinatorialDataScience) over hyp & cst-name incidence matrix

Maybe get score of hyps in ltx via the number of ancesters in the hyp-dag ; then select, among applicable thms of a cluster, those that make use of the highest scored hyp.

For reapeted queries, keep track of which thm was applied to which fvars of the local context, to avoid repeats. Do thsi efficiently somehow.


To prevent metavariable madness : when apply phase, propagate assignement du to goal unification ; in the hyp dag of the applied, the sinks that are isolated can be proven entirely separately ; for connected compo with more then one node, packedge them in iterated PSigmas (?) (essentially exists * , exist * , exists * , ..., *) via topo order of dag, so that only one remaning type without metavariables has to be proven.
Then, if goal is exist or Psigma, try apply phase on components, integrate the ununified ters as psigmas into the initial psigma, and update dependiecies in sigma. Example at `\ex n : Nat, h : 1 \le n, P h`, we could use `le_trans` at `1 \le n` (where maybe we use CEXpr where n is a node ?), so as to get `\ex n : Nat, \ex m : Nat, h1 :1 \le m, h2 : m \le n, P(le_trans h1 h2)`.


backward search : first of all, do the transition graph stuff backwards (ie. one step transition of a goal cluster is all the hype cluster of hyps of thms from the goal cluster ; 2 step closure would be add inner tranisiton layer, in the sense of querying, for a given goal cluster, which goal clusters its hyps lead to). ... Kind of unclear what my whole idea with DAGs for dependencies but keep common list so that one successfull unification of intermed res and goal can lead to fast chained successful unificaions .... Quick note: for types that are function/arrow types, try twice : once as-is and once where fun is added and antectednat is added as hyp. Note that in the latter case, we must use a separate hyp state, as this newly introduce hype exists only in the context of the function/arrow typed goal.

In order to generate terms in backward-proof-search caused by mvars of sensitive type (ie. a type who's type is not prop), we may proceed by memorisation. We take note of the theorems that spawn these requests (by-cases requires a prop to disjoin on, pigeonhole-principle a map to map pigeons with). We build a Trie from these theorem names who's leaves link to objects that will contain the memorised data. This data could be the following : two SetTries/QueryTrees, one for hyp names, the other for goal names, that have indices as leaves. A pair of such indices leads to a list of terms to be used for the mvar, where these terms appear in mathlib proofs in which the request-theorem was used, and in which the goal and the *sampled* hyps lead to the those indices in the two SetTries/QueryTrees.
An important aspect here is that the terms should have their fvars/bvars abstracted, and for us to have a way to replace the abstractions by objects of the same type, from the proof-searh-context.

To consider when using sensitive types:
For samples, store thm implied goal, but also topmost goal ? This should be useful for generation of sensitive types ?

Initial:
For use of empirical weights, maybe select according to diversity ? In the following sense : weight of cluster might be small, but it is large relative to other cluster weights, and generally appears with small weights in other thm ? Seems to lead deep into stats in proba ...
Now:
Doing something to maintain diversity may be a good idea ; completely unclear what this even means


For to_the_moon  and in general: theorems who's assumptions are forall types will never embed in our contexts (unless there was a forall type in the assumptioons) from the start, since we're always retrieving full embeddings (except for instance query). It would be interesting how to obtain partial embedding in our embedding algos (idea : for all combinations of assumptions of a thm, add hypotheses of these type to a fake LCtx, run the embed algo, then when we produce the term, build a function that maps the artificially added assumptions to the term in which the fvars of the fake context corresponding to these assumptions are replaced by appropriate bvars).
Specific idea related to this, based on an example to be generalized, for to_the_moon : in order to use, say `Nat.rec`, in to_the_moon, we could proceed as follows. We may not even need a Nat in ltx to try this, since we can use any constant for the induction to apply on, but it's propably reasonable to try `Nat.rec` only when there's a Nat in ltx. Choose either the base case or the induction step to work on. In the base case, we run to_the_moon up to some arbitrary point where we get a ltx decl that can be eta-factored by `0` : we then let the factor become the motive for `Nat.rec`, and solve the induction step with it. If we start working on the induction step case, ... we're kind of screwed. If we worked with `Nat.casesOn`, we'd introduce some fake `n` to the context (which shouldn't appear in any other context), run to_the_moon up to some arbitrary point where we get a ltx decl that can be eta-factored by `.succ n` (getting `.succ n` into the context is a problem in the first place, as this won't happen if we grow only with theorems, so the `Nat.succ` will never be attempted to embed in the ltx). Then, similarly, we'd get our motive and attempt to prove the base case.


In backward-forward proof search state, store goals in set-trie ? Then top nodes correspond to subgoals required for multiple conclusing-thms to work.

For sensitive types, it may be good to postpone the subgoals that involve the sensitive type. Since we expect that application of a thm wih a particular sensitive type to not get the goal (probably, many need to be tried, hence the name sensitive), we shouldn't wast time of proofs of subgoals involving them, until we#re shore that the proof works with these sensitive types.


