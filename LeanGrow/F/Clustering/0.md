

Adapt names wrt. special support : for example, if an effect of special support is that we get not Iffs ever, then theres not point considering that name when clustering ???

For clusters, notes that mutliple libs can be on same topics : for example Lists in Mathlib.Data, Init, Std.Data or Batteries (depending on version)
