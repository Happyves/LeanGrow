

- For one successful MCTS training session, gather info as follows. For thms transitioning between clusters, score based on clusters. For thms staying in same cluster (ex. Nat.add_assoc and the like), score based on history of thms. The idea is to separate big transitions (cluster to cluster) from low level proofs (that stay in same cluster, such as Nat.add_assoc) 
