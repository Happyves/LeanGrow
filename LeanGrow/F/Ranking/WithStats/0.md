


Find probability of applying thm and presence of set of clusters, conditioned on those clusters by getting it from samples and storing in QueryTree's. That is, the discrimination in the QT should be done with RBNodes instead of tries ; We get probas by temporatily storing number of apps of a set of clusters and a Trie with val the apps for these thms with these clusters -> finally, we get probas by dviding by num of all samples. ADD: we can combined hyp-clusteres and goal clusters as keys in the RBNodes ; either we add the size of one to the other so as to get distincts Nats, or we place them in a pair with a boolean to indicate if its a hyp or goal cluster, then use RBNodes with a lex order

Also, when adding extra weight for thm itself, we should add this ammount when counting thm apps and cluster appearances, so that we truely get probas
