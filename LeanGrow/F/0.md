
# Optimization

Best method : carry out a few reduction menatlly
- if I understood correctly, Monads are slow, and functional version is prefered
- prefer structures to tuples (deep refactors to be done here...)
- use Arrays (but suboptimal if loads of push & pop)
- For my storage system: use syntax of form (((Array.mkEmpty 3).push 1 ).push 2).push 3 (from SciLean) instead of #[1,2,3] to prevent overflow, as length specified ?
- UInt64 and String have special implementations : https://lean-lang.org/functional_programming_in_lean/programs-proofs/special-types.html
- use let for common expressions
- fusion : example -> map f (map g) = map (f g)
- accumulation to avoid tail recursion : use caches that compute intermediate vals in a recursion, instead of going down the full recursion, and then performing all ops
- tupling : return tuple when recursion requires multiple params, so as to avoid multiple calls
- flattening : to avoid multiple recursions, write verion that takes lists of inductive obj as input, and when called, unpacks recusrive substructures, places them in list, and makes single recursive call on list.
- from SciLean : perfrom delta when it makes sense ; apparently if we have some f := (i,j) => i*42+j, and we use it composed as arg, then just inserting i*42+j wil be faster, since no call to the heap for the pair constructor is made ?!?
use `set_option trace.profiler true in`
- make use of C FFI at some parts. For example, ops like taking the intersection of two sorted lists of relatively small nats should be a good use case.
- use @[inline] : Inlining is a core transformation in optimizing compilers. It replaces a function call (call site) with the body of the called function (callee). It helps reduce function call overhead and binary size, and more im- portantly, enables other optimizations.
- use l@(.cons x xs) notation 
- use @[specialize]

# Tracing

- Use my tracing system from the my_project folder > GoodToKnow > Tracing
  Best would be last version, and make separate Trace files for all folders, 

- ask if tracing slows down execution ; with the last system, it souldn't in compiled code, as constant folding should get rid of the ite, but is this true for interpreted code such as tactics ? are they even interrpeted ? In the worst case I just have to manually remove

- Use my tracing system from the my_project folder > GoodToKnow > Profile, with a bit more elaborate form
  Actually, use that from Parallel_3, the other one displays my 0 IQ ; fix time computation as its probably still falso =_=

# random

Maybe try RL for score ? might not be too time consuming ?

Search: for dags, handle the fact that we have the same type in different dags, by having a node idx relative to search tree, and a dictionary that recovers the index in an actual dag, when queried the dag

Maybe simply restrain from applying (backward) thms that would yield holes ; they can be discoverd in a preprocess phase, by checking if the only dag nodes not in the goal type are those of a type that contains only nodes also in the goal type. Then, at unificattion, the types of the remaining goals are known.

Idea for setTrie and discrimi trie mix ; setTrie on top-most constructor (for apps, maybe make system where you also given the number of args ? makes a good razor?) ; there leaves should point to setTries ... to elaborate ...
- for constructors, join their number of appearances ? consider that pair as data of setTrie, to simulate multiset ?
- make iteratively deepening setTrie system : we first build a setTrie that distinguishes on top constructor (smart version, ex apps with nr of args) ; when we get many thms with the same set of top constructors, expand leaf into a setTrie of its own (though, don't mix it with top-constructor-setTrie), that does the same, but for the set of 2nd layer constructor. Their is no notion of order, and we hope that the structure of the expressions are sufficiently different so that this discirmination method is viable.



# Schedule :


- October : work on a system do grow down from goal -> forward-backward ; triggers for tactics ; tracing system that allows to store partial progress, and system to load partial progress (if randomization is used, make sure to add random vals in trace and add system that follows these vals to display reproducability) ; explore possibility of parallelisation/concurrency
- November : make a frontend (+ system that allows for adding caches of personal projects ; + version that automatically build clusters and all (or maybe only essential) wrt. declaration of same file that the grow tactic is run in, so as to allow easy "online" API writing) ; clean, build, debug, bench
- December : go public (no preprint ?) & in parallel, perform last attempts to do something theoretically interesting on SetTries


# Order 

Thourough investigation of reduction
- Investigate effects of expr reduction : it reduces Nat.brec, but does it perform other delta's ??? Note case for reduces Nat.pow 2 !


SpecialSupport > Clustering (names like Iff or And relevant for culsers ?)

Search > SampleSensitiveTypes

# For paper


Reverse dags in embeddings so that sinks get explored first, so that the propagation phase is longer ?
-> example of (x1 x2 ... xn : Nat) (h : (x1 + x2) + ... xn = 42) to be match with (y1 y2 ... yn : Nat) (h : (y1 + y2) + ... yn = 42). If we match the Nats first, we get n! possibilities, whereas maching the h's yields the embedding in a single propagation phase

For memorisation of sensitive types, appeal to von Neumanns quote: math isn't learnt, its assimlated

# Future

- for tranisition graphs use spanners, spasification, or skeleton techniques to reduces size ?


# Technical debt

- In `Lean > Meta > ExprDefEq`, there is the case of `Lean.Meta.isDefEqProj.isDefEqSingleton` which I don't intend to implement, 
  at least in a first run. It reduces object of a type that is  a structure with a single entry to  that entry.

- At reduction of CExpr with iota, at CExpr.toCtorIfLit, we ignore String case