

When we carry out the rewrite by adding equivalent pattern to tree, duplicate all labels, so that it corresponds to rewriting that pattern at every term having it at that location. Indices will grow tremendously. Also, we should avoid building all alternatives : this will require inferType and whnf to build from the cexprtrie on demand ???

In the context of \u, if we expect indices to come in intervals, maybe, instead of using lists of indices, use list of intervals, where intervals are specified by a pair of points ?

When using CExprTries as discriminaiton keys in SetTries:
We should only consider, for a given thm, the hypotheses that require a term that is not a sort. The reason is that, for example, for polymorphic thms with a hypothesis `a : Type _`, if queried in a context where hyps are satisfied for a concrete type for `a`, then the query will still fail as `a : Type _` won't be *explicitely* in the query.

perhaps better to have branches that correspond to constructors and a node-type branch which just stores a list of branches, indicatig multiple choices