

**BIG** : at query retrieve list of sub-trees for future queries (done queryWret)


Add queries up to a number or ratio of misses. For example, we can query with a counter allowing for at most n matches (recurse, dreasing counter if miss but keep going, only stop if counter reached 0). Shouls return subsets that differ in at most n entries to the query (in the sense |res \  query|, not the symmetric difference). For ratios, we'll need to add size information at each subnode, I guess ? (to think)
If we do this for state search, then we may end up with positive response for a state in which hypotheses may be missing, that are required by thms that is suggested in this state.


Durring querytree queries, recover depth of returned leaf data ; this could be used for ranking, as large depth => large overlap of thm names ; note that this will introduce a similar bias as for ranking by weighted "hyp-applied" criterion, in that we'll miss on theorems that apply but use few but common constants (like Eq)...


To find pattern in a collection of large expressions, we could first derive the leaves of the expressions (const, bvars, lit, ...). Then we consider the leaves of the pattern, and apply as first trimming criterion the following: the set of leaves of the pattern should be a subset of that of the expression, hence if we store the expressions in a set-trie based on these leaves sets, the query may become efficient.
