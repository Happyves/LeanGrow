
Add a reference counting system to handle deletions. It should just be the number of terms that have this constructor at that place. If it reaches 0, we delete the branch. Every time we add a term, increase the counter at all constructor-branches the term correpsonds to by 1.

perhaps, instead of rwDirs, we can use indices pointing to parts of CExprTries instead ?

apparently took me ages to realize delta is a big deal, else a constant and its def wont unify...

random note: iota corresponds to rws in `.eq_1`-thms, and delta to rws in `.unfold_eq`-thms.

TO FIX : at query and all files design from it, if the aomic constructors aren't found, and no rewrite branches are found, we should fail and stop  the recursion, though we currently don't. This is what happens if one test the algos only on positive examples, and not on negative ones :<

Problem: if we were to run grow on a query in which there is `(a : b = c)` in the ltx, we'd want to insert RW-nodes in the CExprTrie where `b` or `c` occur. We may have to do this after rewrites, as if `b` is a complex pattern, it may only appear after rewrites.

We did't develop a way to merge rw-classes yet. If a pattern for a rw is found at the top of a rw-class, then we should integrate this to the rw class somehow...