

Make a pretty printer for CExps so that traces become readable

For reduction: couldn't we just apply the trick with adding whnf and inferType as inputs in the steps, then adding the actual functions durring their implementation ? This should work and result in partial functions accepted by Lean. =_= comes a bit late...
