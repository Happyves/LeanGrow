

# TODO

separate .lnode _ _ .none and .lnode _ _ (.some _) into different CExpr constructors, and replace tagging by replacing one constructor by the other.


# Technical debt

 
# Notes

Durring processing of thms, split iffs into two implications, so that we don't have to spam And.left and And.right constantly (ie. make 2 versions, where in each, one side of the impli is treated as a hyp, so as to get embedding to work)
Also, for equalities, do the same, and use `Eq.rec` instead of `Iff.mp` when applying (make sure to add info on how to apply in cache format).
In both cases make verison where this is not done, as eqs and iffs may be actual goals or hyps as-is.


Investigate partial embeddings ; useful for example for simple suggestion tactic, and may be useful for other search stuff, say if SpecialSupport turns out not to be able to handle implications well...


`mkAppM` solves all instance and univers problems, it seems, lol ; actually, it causes problems in our framework since it'll complain if we provide it implicit args that aren't instances, which we do. So maybe, for embeds, store info if hyp is explicit or not, and use `mkAppM` in what follows, or make a modified verison of it that only synthesises instances

Maybe join pdata and gdata since we require both name sets in both cases ?



Big idea: linearize cexprs ; at constructors with multiple args, store list of positions to jump to, where the args start ; we should be able to do unification in this format ; store via a list of indexed tries ; for example, the top trie will have an edge for `app`, which will lead to a leaf who's entry points to two further tries of the list