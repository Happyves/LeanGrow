

for fast-API-writing-version, simply add all declarations of the same file as highly score thms

make version that returns the built local context even if proof search failed, so as to still be useful

There will probably be definitions that aren't supported in the query, such as defs we might skip (match stuff for reduction, potentially), or defs that haven't been cached. We should identify them and warn the user about it.